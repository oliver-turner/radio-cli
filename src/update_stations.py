# Add a function that will update the object based on the stationuuid
# Most likely when the url_resolved gets changed
# need to be able to sanitise HTTP to HTTPS
# Check if there is an equivalency to the current store, if not then update?
